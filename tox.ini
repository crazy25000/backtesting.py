[tox]
envlist = py39
requires =
	tox-pyenv

[pytest]
testpaths =
    backtesting

[testenv]
deps =
    pytest
    scikit-optimize
commands =
    pytest

[testenv:clean]
deps = coverage
skip_install = true
commands = coverage erase
